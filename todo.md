# RothInfoStrings open work

- Validate login and zone-change updates on the target Midnight build.
- Verify settings, saved placement and reload persistence.
- Confirm the optional `RothLib` integration is either available or safely absent.
- Run a focused safe-formatting check for secret/proxy values.
